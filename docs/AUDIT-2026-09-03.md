# breakout-team 전체 검증 — 2026-09-03

실행 이력 20회(2026-08-05 ~ 09-03), 코드 전체, 워크플로 프롬프트, 산출물(리포트·대시보드)을 놓고 검증했다.
1~6장은 읽기 전용 감사 결과이고, 구현은 2026-09-04 새벽에 끝났다 — 아래 "구현 상태" 참조.

---

## 구현 상태 (2026-09-04)

| 항목 | 상태 | 구현 위치 |
|---|---|---|
| 1-1 야후 봉 누락·세션 불일치 | ✅ | `lib/market-calendar.js`(신규, NYSE 휴장일 2025~27) · `bars.js barQuality` · `run-breakout.js barsNotice` → team1.data_source·chief·리포트 상단·대시보드 배너 |
| 1-2 재편입 churn | ✅ | `tracking.js revertReentries` — 배제 사유 재검사 후 되돌림(히스토리 없이, `reentryBlocked` 기록). chartCheck·breakouts 는 최종 status 로 필터. peak 는 첫 평가 때 야후 종가로 초기화 |
| 1-3 우선주·유닛 | ✅ | `run-breakout.js hasMarketCap` — 시총 N/A 행을 4팀 후보에서 제외(`excludedNoMarketCap`). 9/04 실측 10개 제외 |
| 1-4 돌파봉 거래량 | ✅ | `ta.js priorHighBreak.breakVolRatio`(돌파봉/직전 20봉 평균). RS 컬럼은 돌파일 = 세션일일 때만 |
| 1-5 세션일 | ✅ | `sessionDateFromUtc` — 시계열 동기화·돌파 판정·실장 인자에 사용 |
| 1-6 0종목 push | ✅ | `run-breakout.js` rows.length===0 → throw |
| 1-7 팩트체크 미매칭 | ✅ | team2·team4 idx 기반 대조, 미매칭은 `unchecked` |
| 1-8 5팀 업종명 오염 | ✅ | `team5-sector.js` 결과 key/industry 를 입력값으로 덮음 |
| 1-9 대시보드 | ✅ | 실장 결과·dataNotice·barsNotice·fetchedAt·researchedOn·재편입 차단·베이스 상하단·LLM 종합 3세트 렌더 |
| 1-10 조용한 상한 | ✅ | chartCheck `total/shown/beyondCap` · 리포트 표 전량 · `dropped[].asOf`(ET 봉 날짜) · 4팀 `ineligible` 커버리지 · 5팀 `poolTotal` · 실장 `picksTotal/notableTotal/chartCheckTotal` |
| 1-11 기타 | ✅ | tracking.save 를 산출물 직전으로 · `dropped_today` 를 `excludedAt===오늘` 로 파생(멱등) · `summary.unevaluated` · detail 캐시 도장은 수집 성공 시만 · `byPhase.unknown` · `-verified.json` 은 report 만 저장 |
| 2 토큰 | ✅ | `research-rotation.js selectForResearch` — 신규·TTL 경과·변화(돌파·차트확인·8-K·국면)만. `run-breakout.js` 가 research/catalyst/업종 리서치를 이월(`researchedOn`·`carried`). team5 버킷 추가. 4팀 ⑥ TTL 내 재조사 제외. `_t2/*.json` 슬림. 프롬프트 범위→최대치, "문장마다 숫자" 삭제, 제공 자료 우선·웹검색 ≤6회, 종합 3개 sonnet |
| 3 서술 기준 | ✅ | 워크플로 5개 STYLE 블록, `plain-terms.js` 삭제, `briefing-plain.test.js` 내부 코드 유출 검사만, CLAUDE.md·RESUME.md·start-breakout.md 정정 |
| 4 원천 데이터 | ✅ | `run-breakout.js buildReport` 전면 개편(FINRA 절대값·13개월·free credit·순레버리지, 2팀 전량 표 + RS 원값·수익률·DIV·CY/NY·Up/Dn·BBWTHD·저항/지지, 3팀 활성 57 표, 4팀 전량 표 + 베이스, 5팀 상위2%·섹터·자금흐름 83업종 표). `build-chief-report.js` 📚 절(1팀 뉴스·리서치 종목별 SEC 4분기 표·증권사 리포트·8-K·촉매 12종목·업종 근거·테마 종합). 실장 인자 `--team1`·financials·count 필드 |

**2026-09-04 새벽 실행에서 추가로 잡은 것 (감사 후 발생):** RS 사이트 스냅샷의 616행(43.6%)이 200DIV·50DIV·RS 전부 `null` 이고 `Above_150_SMA` 만 `X` 였다. 기존 `ma-guard` 는 200DIV 가 null 이면 교차검증을 건너뛰어 통과시켰고(그리고 `Number(null)===0` 버그로 null 을 0 으로 읽었다), 3팀이 **26종목을 "150일선 이탈"로 영구 배제**했다. tracking.json 을 git HEAD 로 되돌린 뒤 `ma-guard.isBlankRow / aboveMa150Of / BLANK_MAX=15%` 를 추가 — 빈 행의 X 는 "이탈"이 아니라 "판정불가"다. 재실행 결과 오늘 탈락 0, 활성 57/배제 51 유지. 회귀 테스트 `tests/lib/guards.test.js`.

**검증:** `node tests/run-all.js` 8개 파일 전부 통과(신규 guards 포함). `run-breakout --no-git` → `prepare-llm-args` → `build-chief-report --no-git`(9/03 페이로드로 병합 시험) 완주. 9/04 첫 `start breakout` 은 이월 캐시가 비어 있어 에이전트 수가 아직 많다(2팀 18·4팀 11·5팀 6). 이틀째부터 줄어든다.

독자 기준 변경: 사용자는 재무·회계 전공의 금융 실무자다. "중학생도 이해할 수 있게" 규칙은 폐기 대상이고,
**원천 수치 누락 없음**이 새 기준이다.

---

## 0. 한 장 요약

| 영역 | 판정 | 핵심 |
|---|---|---|
| 숫자 정확성 | ⚠️ 결함 2건이 실제 오보를 냈다 | 야후 봉 8/28 누락(데드크로스 날짜·50일선 연속일 오염), 3팀 재편입 churn(배제 10건 중 대부분이 매일 되살아난 종목) |
| 환각 방어 | ⚠️ 1건 | 4팀이 우선주·유닛(NEE-T 등)을 보통주로 조사 |
| 토큰 | 🔴 하루 약 300만 중 절반이 구조적 낭비 | 2팀 68%·5팀 87%가 TTL 안 재조사. 로테이션이 "순서"만 바꾸고 "건너뛰기"를 안 함 |
| 원천 데이터 | 🔴 리포트 본문에 재무제표·증권사 리포트·8-K·4팀 촉매·5팀 근거·자금흐름 표가 0줄 | 팝업에만 있음. LLM 종합 필드 3세트는 어디에도 표시 안 됨 |
| 서술 수준 | 교체 필요 | 프롬프트 5개·plain-terms.js·테스트 1개·후처리 1곳 |
| 이미 견고한 것 | ✅ | 백분위, WRS 재계산, ma-guard, 상한 마커(3팀 breakouts), verify-claims, tryAgent, 원자적 쓰기 |

---

## 1. 결함 — 실제로 틀린 숫자를 냈거나 낼 수 있는 것

### 1-1. 야후 일봉 8/28 누락이 전 종목에서 조용히 통과했다 (재확인 완료)

- `%LOCALAPPDATA%\breakout-team\bars\2026-08-31\`과 `2026-09-01\` 캐시에 **2026-08-28 봉이 없다**(QQQ n=499, 8/27 → 8/31). 9/02부터 복귀(n=501).
- 결과: 9/01 실행이 "데드크로스 8/31 D+0", 9/02 실행이 "8/28 D+2"로 **같은 사건의 날짜가 바뀌었다.** 로직 문제가 아니라 데이터 갭이다.
- 3팀 `consecutiveDaysBelowMA`는 봉 개수를 세므로 갭만큼 과소 계산. AGL 히스토리에 8/28과 8/31이 같은 "16일 연속(~08-27)"으로 기록됨.
- 원인: `scripts/lib/ta.js:40`이 `close == null` 봉을 버리고, `scripts/lib/bars.js`가 봉 연속성·마지막 봉 날짜를 검사하지 않는다.
- 수정: `fetchBars`가 버린 봉 수와 `lastBarDate`, 직전 봉과의 거래일 간격(≥2면 `gap:true`)을 반환. QQQ의 마지막 봉이 기대 세션일과 다르면 team1·chief·대시보드에 "봉 누락 — 판정 신뢰 불가" 배너.

### 1-2. 3팀 재편입 → 즉시 재배제 churn (재확인 완료)

- `tracking.ingestPicks`(`scripts/lib/tracking.js:67-77`)는 2팀이 다시 뽑기만 하면 배제 사유가 여전히 성립해도 `active`로 복귀시킨다. 2팀 기준에 50일선이 없으므로 50일선 아래 RS 상위주는 **매일** 복귀 → 같은 날 `evaluate`가 재배제.
- 실측 `state/tracking.json`: MXL·BAND·PENG 복귀 29회/배제 29회, ALAB·MRVL 26회, AMD 22회, AGL 20회, SNDK 18회 … 18종목이 5회 이상.
- 파급 (a) 9/03 `dropped_today` 10건 대부분이 churn이라 진짜 신규 배제(ORKA 등)와 구분 불가. 실장 인자·리포트 "오늘 배제"·`flowCross.agreement.droppedTotal` 전부 부풀림.
- 파급 (b) **chart-check 오염**: `run-breakout.js:503-508`이 `_ctx3`를 최종 status 확인 없이 순회 → 오늘 AGL·STX·ATEX·SNDK가 "차트 볼 종목"에 실림. `breakouts`(264)·`items[].live`(279)도 같은 경로.
- 파급 (c) 복귀마다 `peak = p.price` 리셋(tracking.js:72) → 배제조건 ②(고점 대비 -40%)가 이 종목들엔 영구 무력화.
- 파급 (d) `pushHistory` 60개 상한(tracking.js:44)에 밀려 AGL 8/05 원 기록이 잘려나가는 중.
- 수정: ingest 후 `restored` 종목에 배제 3조건을 먼저 재검사해 여전히 걸리면 히스토리 없이 excluded로 되돌리고 `reentryBlocked:{date,reason}`만 갱신. chartCheck/breakouts/items.live는 `trackState.items` 최종 status로 필터.

### 1-3. 4팀이 우선주·유닛을 보통주로 조사한다 (환각 인접)

- 오늘 거래량 1위로 조사된 **NEE-T**는 유니버스에서 시총 N/A인 티커인데, 에이전트는 넥스트에라 에너지 보통주(도미니언 인수 주총)로 조사해 `pass` 판정으로 리포트에 실렸다. BA-A·CTA-A·ALB-A·VSECU·BTSGU도 오늘 후보에 있다.
- `state/research-cache.json` team4에 NEE-T·NEE-S·HPE-C·KIM-N·MAA-I·BAC-L·SPG-J·HL-B·SLMBP(6회)·MCHPP(4회)·SMCIP·SENEB(8회)·GOOGN·BATRA·STRF 등 ≈25/140.
- 이 티커들의 `_t4/*.json` 뉴스는 전부 `direct:false`(말레이시아 증시·코코아 시황) → 근거 0으로 웹검색만 하게 된다. 20회 중 ⑥ "촉매 없음" 판정이 전체 217건 중 68건(31%).
- 수정: `prepare-llm-args.js:86-89`에서 `/[-.][A-Z]$/`, `/P$|U$/`(화이트리스트 예외) 또는 `marketCap==null`을 제외. `_t4/*.json`에 `direct:false` 뉴스를 쓰지 않는다. 5거래일 내 ⑥ 판정 종목은 재조사 제외.

### 1-4. 거래량 확인이 "돌파 봉"이 아니라 "마지막 봉"을 본다 (재확인 완료)

- `run-breakout.js:265` `volOk`는 `volx`(RS 당일)·`volSurgeWk`·`volRatio5`(ta.js:195 `last.v`) — 전부 마지막 봉. `breakDate`는 최근 5봉 안에서 찾는다.
- 실측: TARS breakDate 8/31인데 `volumeConfirmed:true` 근거는 9/02 거래량(volx 2.79). OKTA 8/27 돌파, 9/02 volRatio5 0.54로 "미확인".
- 수정: `priorHighBreak`가 `breakIdx`를 반환하고 `bars[breakIdx].v / sma20(v, breakIdx-1)`을 `breakVolRatio`로 계산. RS 컬럼은 breakDate가 마지막 봉과 같을 때만.

### 1-5. RS 세션일과 야후 봉 날짜의 불일치를 표시하지 않는다

- 유일한 검사는 `last_updated` 4일 초과(fetch-rs-data.js:72). 9/01 09:29 KST 실행의 RS는 `2026-08-31 01:32 UTC` = 8/28 세션인데 봉은 8/31까지 → VOL_X·Above_150(8/28)와 belowMa50·brk(8/31)를 섞어 판정, `stale:false`.
- `dataDay = last_updated.slice(0,10)`(run-breakout.js:497)은 렌더 시각의 UTC 날짜. 9/03 두 번째 실행은 `09-03 00:01 UTC`(9/02 세션) → 시계열에 09-03 지점 추가, `series/AGL.js` 마지막 점 가격 null.
- 수정: 세션일 = ET 기준 `(last_updated − 2h)`의 날짜. QQQ `lastBarDate`와 다르면 `data_source.sessionMismatch` 배너.

### 1-6. RS 사이트 장애 시 0종목으로 "정상 완료"하고 push한다

- `paths.sampleData`(`sample-rs-data.json`)가 존재하지 않는다. 라이브 실패 → `rows=[]`, `stale:null` → 2팀 0종목, history.js picks:0, 대시보드 덮어쓰기, 커밋·push.
- 수정: `rows.length===0`이면 throw. `--allow-sample` 명시 시에만 계속.

### 1-7. 2·4팀 팩트체크 미매칭이 조용히 통과한다

- `team2-research.js:207-210`, `team4-catalyst.js:178-181`은 `r.ticker` 문자열로 대조하고 없으면 `continue`. 5팀(191-197)만 `unchecked`로 표시한다(8/25 정유 사고 후 고침). 2·4팀은 같은 결함을 그대로 갖고 있다.
- 수정: 배치에 인덱스를 넣고 인덱스로 대조, 미매칭은 `unchecked`.

### 1-8. 5팀 업종명이 오염되어 캐시 키가 갈라진다

- 스키마가 `key/industry`를 required로 두지만 모델이 고쳐 쓴다. 이력에 같은 업종이 "Technology|Computer Hardware", "technology-computer-hardware", "tech-computer-hardware" 등 10개 이상 변형. 오늘 "Healthcare / Biotechnology (헬스케어-바이오테크놀로지)".
- 수정: `parallel` 후 `analyzed[i].key/industry = targets[i].key/industry`로 덮어쓴다(인덱스 기반).

### 1-9. 대시보드가 세 가지를 표시하지 않는다

- `dataNotice`(150일선 오염일 배너) — 9/02 실제 오염일에 2팀 탭은 41종목을 통과분처럼 표시. HTML에 `dataNotice` 0회.
- `detail.fetchedAt`(이월 자료) — 9/03 37종목 중 17종목이 9/02 자료인데 구분 없음. HTML에 `fetchedAt` 0회.
- **실장 LLM 결과** — `breakout-room.html:207`은 `c.llm.note`만 렌더 → 병합 후에도 종합 탭이 "대기". headline/todayFocus/teamSummaries/tomorrowWatch가 대시보드 본체에 없다(index.html에만).

### 1-10. 조용한 상한·되돌림 (빠진 것이 빠진 티가 안 나는 자리)

| 위치 | 현상 |
|---|---|
| `run-breakout.js:519` | chart-check `eyeScore≥5` 21개 → `slice(0,10)`, 총량 표시 없음. history.js에 매일 정확히 10 |
| `run-breakout.js:652` | 리포트 2팀 표 `picks.slice(0,30)` — 37종목 중 GWRE·NUTX·FROG 등 7개 누락, 그중 3개는 리서치 완료 종목 |
| `run-breakout.js:219` | `q.research = pending` 매일 초기화 → 캐시상 37/37 조사 완료인데 화면은 done 20 / pending 17 ("순환 조사 대기") |
| `prepare-llm-args.js:169` | `dropped[].date = t1.generated`(KST 실행일) vs `breakDate`(ET 봉 날짜) → 같은 세션의 돌파·배제가 "9/02 돌파 → 9/03 배제"로 읽힘 |
| `prepare-llm-args.js:86` + `util.js:259` | 4팀 후보 45 중 적격 12, 나머지 33은 로테이션 대상이 아닌데 커버리지 문구는 "다음 실행부터 순환 조사" |
| `prepare-llm-args.js:149` | 5팀 `coverage.total`이 슬라이스 후 6 → 항상 6/6 |
| `prepare-llm-args.js:487,503,37` | 실장 `picks.slice(0,15)`, `notable.slice(0,10)`, 1팀 `candidates.slice(0,22)` — 총량 없음 (3팀 breakouts만 `breakoutTotal` 있음) |

### 1-11. 그 외 (영향 작지만 고칠 것)

- `run-breakout.js:259` tracking.save가 대시보드 쓰기보다 먼저 → 후반 실패 후 재실행하면 `dropped_today: []`. `dropped_today`를 `items.filter(excludedAt===dateStr)`로 파생(멱등).
- 활성인데 봉이 없는 종목(HPE-C, 8/05 편입 후 평가 0회)이 집계에 없다 → `summary.unevaluated`.
- `detail` 캐시 도장이 하위 수집 전부 실패해도 찍힌다(run-breakout.js:434-462) → 근거 0인 채 LLM 대상.
- 하루 두 번 실행하면 로테이션이 다른 20종목을 뽑아 LLM 비용 2배(9/03 실제). 같은 `dateStr`이면 이전 targets 재사용.
- `congestion.js:158-165` `bounce_trigger`가 리테스트 후 무기한 유지 → 창(≤5봉) 필요.
- `tracking.js:56,72` peak가 RS 원본가, 비교는 야후 조정가 → 액면분할 시 -75% 오배제. peak도 봉 종가로.
- `detectCongestion` 봉 부족(<70)이 `phase:'none'`으로 합산 → `byPhase.unknown` 분리.
- `--date=`는 라벨만 바꾼다(봉·tracking은 오늘 기준).
- `verify-claims.js:161` 날짜에 시각이 붙으면 당일 기사가 "미래"로 제거.
- `build-chief-report.js:84` `d.generated===dateStr` 검사 없음 → run-breakout 실패한 날 어제 team2.js에 오늘 LLM 결과 병합.
- `tracking.js:57` bestPct null → "RS 상위 100%" 기록.
- `.claude/commands/start-breakout.md` 3단계가 `--team5=`만 적고 있음(실제 운영은 `--team2/--team4`도 넘김). 문서 정정.
- 중복 계산: `rankPercentiles` 4회, `analyze(b)` 최대 3회, `computeWrsAll` 3회, QQQ 봉 2회. ms 단위라 우선순위 낮음.
- `state/llm-in/{date}-verified.json`이 payload 복제본 → llm-in 17MB, git pack 14.9MB. `dashboard/charts/qqq-*.png` 22개 누적.

---

## 2. 토큰 — 하루 약 300만 → 150만

9/03 실측: team2 1,642,775 (에이전트 25, 도구 479회, 23분) · team4 808,908 (16, 116) · team5 511,287 (9, 114) · chief 61,050.
8/13 기준선(리포트형 확장 전): team2 558k · team4 436k · team5 274k.

| 항목 | 현상 | 수정 | 절감 |
|---|---|---|---|
| **2팀 TTL 안 재조사** | 직전 5회 실행 안에 조사한 종목을 다시 조사한 비율 **68%**. 9/03은 20개 중 18개. `research-cache` WGS 16회·SNDK 15회·DELL/TEAM/TWST 13회(5거래일 TTL이면 ≤4회). 원인: `research-rotation.js`가 정렬만 하고 cap 20을 항상 채움. 후보 37개를 하루 20개씩 돌리니 모든 종목이 이틀에 한 번 처음부터. | 정렬 뒤 **stale ≥ TTL 또는 신규 또는 변화(새 8-K 2.02·신규 돌파·chartCheck 진입)** 인 항목만 남긴다. cap은 상한이지 목표가 아니다. 나머지는 어제 결과를 `researchedOn` 붙여 이월. 정상 상태 ≈ 7~10개/일 | ~80만 |
| **5팀 캐시 없음** | 재조사율 **87%**. Computer Hardware·정유·전자유통은 20회 중 14회 이상. `research-rotation.js`에 team5 버킷이 없고 `prepare-llm-args.js:128-152`는 매일 6개 전부. 근거 자료가 members 헤드라인뿐(`members:[]`인 업종 다수)이라 검색 19회/업종, 팩트체크 제거율 32%로 최고. | team5 버킷(TTL 5, 키=`industry.key`). 신규 진입·순위 변동 N 초과만 재조사. Node의 `flow.industries` 행(FRANK/f10/f25/d50/d200/stage/flow)을 넘겨 데이터에서 출발하게 | ~30만 |
| **문장마다 숫자 강제** | `team2:101`, `team4:94`, `team5:76` "각 문장에 숫자·날짜 최소 하나" → 팩트체크가 숫자 불일치로 제거(5팀 32%·4팀 19%·2팀 6~40%). 8/26은 87개 중 44개 제거. 범위형 상한(3~6개, 4~6문장)이 목표로 읽혀 whyRose 평균 5.3, 출처 11.8개/종목, 검색 24회/종목 | "숫자는 quote·제공 자료에 있는 것만, 없으면 숫자 없이 서술". 범위 → 최대치(whyRose ≤4, counterpoint ≤2, estimateRevisions ≤2). "제공 자료에서 먼저 채우고 부족한 항목만 웹검색 ≤6회" | ~30만 |
| **4팀 우선주·⑥ 재조사** | 1-3 참조. `_t4/*.json`이 `financials`·`krReports`를 안 실어 ①어닝 종목(CRDO·DELL·MDB)도 SEC 실적표 없이 검색 | 우선주 제외, direct:false 뉴스 제거, 2팀 detail의 financials/krReports 동봉 | ~13만 |
| 파일 다이어트 | `_t2/*.json`의 미사용 키(financials.tags/cik/source_url, news.tickers/tier1, krReports.secureId/pages/opinion, filings.description) 20~30%. `team2-research.js:112-118` 인라인 빌더는 argsDir 모드에서 dead code | 인라인 경로와 같은 상한(뉴스 6·리포트 5·공시 4)으로 슬림 파일 | ~2만 |
| 팩트체크 | haiku, 배치 6/5/3. `team4:155`가 company/volumeExplanation까지 재삽입. 프롬프트가 "웹 접근 없이 대조"를 명시하지 않아 URL 재fetch 가능성 | "텍스트 대조만", claims+narrative만 전달. 개발 일화("2026-08-14 실측…") 3파일 중복 → 주석으로 | ~3만 |
| 모델 | 4팀 촉매종합(`team4:234`)·5팀 섹터종합(`team5:226`)·2팀 테마종합(`team2:267`)은 주어진 JSON 재구성인데 opus | sonnet. 실장만 opus | 비용 5× |
| `tryAgent` | 재시도가 전체 프롬프트 재실행, 도구 예산 없음 | 에이전트당 도구 상한(≈10 fetch) | — |
| 정리 | `_reuse.json`(150KB, 8/09), `_t2args.json`(582KB, 8/14), `_t1slim.json` — 읽는 코드 없음 | 삭제 | — |

---

## 3. 원천 데이터 누락 — 계산은 되는데 안 보이는 것

### 3-1. 마크다운 리포트에 0줄인 것 (팝업에만 있음)

1. **SEC 4분기 재무** — 37종목 전부 `detail.financials`(revenue/영업이익/순이익/마진/YoY, 유도분기 표시) 보유. 리포트는 LLM 산문에서 "매출 17억…"으로 언급할 뿐 표가 없다. 실장도 못 본다. → `build-chief-report.js:199` 회사 설명 뒤에 4분기 표, `prepare-chief-args.js:56` llmResearched에 최근 분기 revenue/yoy.
2. **국내 증권사 리포트**(증권사·애널리스트·날짜·요약·PDF)·**8-K 공시** — 팝업 L934-955만. 요약은 220자로 잘림. → `build-chief-report.js:224` 뒤.
3. **4팀 촉매 분류 12종목**(categoryName/company/volumeExplanation/claims+출처) — `build-chief-report.js`가 team4.js에 병합만 하고 리포트 절을 안 만든다. 리포트 4팀 절은 base 5종목 표뿐. → L255 이후 신규 절.
4. **5팀 강세 사유 6업종**(lead/whyStrong/risk/driver/durability)과 **자금흐름 139업종 표**(FRANK/frank25/f10/f25/d200Δ/stage/flow) — `run-breakout.js:693-703`은 strictTop2.m6 2행만. 실장 산문의 "14위·4위"를 검증할 표가 없다. → flow∈{leading,inflow,narrow,outflow,pending} 79개 표.
5. **애널리스트 추정치 수치**(cyTrend/nyTrend/upCount/downCount/upDownRatio) — 대시보드 2팀 탭에만. 실장은 업종 중앙값만 받는다. → 2팀 표에 CY/NY/Up/Dn 열, `prepare-llm-args.js:161-166` top 배열에 추가. ※ 계획서에 있던 "epsTrend 현재 vs 90일 전", "upgrade/downgrade 이력"은 **코드 어디에도 구현돼 있지 않다**(grep 0건). 있는 것은 RS 사이트 CY/NY_Trend와 Up/Down_Count, LLM 산문뿐.
6. **FINRA 원자료** — "YoY +38.6%" 1줄. 최신/전년 절대금액(1,417,225 / 1,022,548 M$), 13개월 시계열, **free credit(cash·margin) 잔고**(net leverage 계산 가능)가 어디에도 숫자표로 없다. free credit은 완전 dead.
7. **3팀 추적 표 108종목**(peak/drawdownPct/belowMa50Days/eyeScore) — 리포트에 배제 10·돌파 11만. 활성 57종목의 고점 대비 낙폭이 안 보인다.
8. **RS 원값(rs.*.v), 절대수익률(ret*), 기간 최대상승(maxRise*), 10DIV/50DIV, BBWTHD/LOW, Order/정배열, 저항·지지선(ta.*)** — 리포트 표는 백분위만.
9. `research.upcomingCatalyst`(다음 실적발표일 등 15종목)·`confidence`·`themeTags` — 리포트 0줄.
10. 3팀 `breakouts[].volRatio5` — 판정에는 쓰이는데 사람 눈엔 안 보임(실장만 받음).

### 3-2. 매일 LLM이 생성·검증하는데 어디에도 표시 안 되는 것 (완전 dead)

- team1 `marketNarrative` / `keyRisks(6)` / `easySummary(3)` — 실장에게도 안 감(`prepare-chief-args.js`에 `--team1` 없음)
- team4 `summary.sectorSignal` / `watchList(6)` / `caution`
- team5 `summary.rotationView` / `strongest` / `emerging` / `fading` / `caution`, 업종별 `risk`
- team2 `theme.leadingTheme/subThemes/crossCuttingDriver/caution` — index.html에만
- 실장 `tomorrowWatch` — 리포트에만, 대시보드 둘 다 없음
- `verified.removed` 27건 목록 — 카운트만. 어떤 근거가 왜 빠졌는지 추적 불가

### 3-3. Node가 계산·저장하지만 아무도 읽지 않는 것

- team1: `qqq.index_dip`(200DIV 8.02%, 눌림목 판정), `finra.*.freeCreditCash/Margin`, `leaders.stocks[].div50/price/rs.m1·m3·m6`, `market_overheat.reentry/max/median/count`(재진입권 -20% 이하 54종목, 중앙값 5.37%)
- team2: `marketCap, div10, div50, bbwthdLow, order, jeongbae, aboveMa50, clsPos, brk60d, ta.{resistance,support,contraction,trend}, wrs.rankPct6, ma150SlopeDir, research.factcheck`
- team3: `peakDate, excludedAt, live.congestion, ingest, cap`
- team4: `price, adr, high52, clsPos, brk60d`(대시보드 표 미표시), congestion `baseHigh/baseLow/baseBars/contraction/distToPivotPct/aboveMa150AtNow`(베이스 하단 = 손절 후보가 안 보임), `catalyst.isHighlight/factcheck`
- team5: `final.*`(Final_WRS), `sectors[].sum6`, flow `siteF25/siteToday/axes/d200Prev`
- chief: `flowCross.outflow[].droppedToday`, `picks[].why`, `llm.verified.removed`
- universe.js rows `d10, vw, ord, bb, bbl, cls, brk` — 검색 팝업이 안 읽음
- chartcheck `reasons`가 문자열뿐 — 저항가·BB폭·거래량비가 구조화 필드로 없음(`vcp.js:97-102`는 `flags`도 반환하는데 `run-breakout.js:518`에서 버림)

### 3-4. 실장이 받지 못하는 것

`_chiefargs.json`(39KB)에 없음: team1 LLM 뉴스 전체, team2 theme·company·lead·confidence, team4 summary, team5 summary·risk, 3팀 활성 목록, weekly_question, QQQ slope/sweetSpot, FINRA 절대금액·시계열, 37종목 중 22개(top 15 + 조사 20만). 그런데 스키마는 `teamSummaries.team1..team5`를 요구 → 1·4·5팀 요약을 숫자만 보고 쓴다.
프롬프트가 참조하지 않는 것(있는데 안 씀): team1.leaders/sectors/overheat/goldenCross, team2.stats/top, team4.notable.trigger, team5.sectors, chartCheck.reasons, flowCross.agreement/breakoutsInInflow.
`chief-report.js:46` "`teams.team2.research`의 status는 믿지 마라"는 파일에 없는 필드를 가리키는 낡은 지시.

---

## 4. 서술 기준 교체 — "중학생" → 금융 실무자

### 4-1. 규칙이 있는 곳 전부

| 파일 | 줄 | 내용 |
|---|---|---|
| `scripts/workflows/team1-news.js` | 145, 170, 175, 177, 192, 212-213 | 중학생·비유·초보자용 3줄·"아주 쉽게". 170(4~6문장)과 212(3~5문장)가 서로 모순 |
| `scripts/workflows/team2-research.js` | 73, 104-106, 131-132, 226, 233, 254-260 | company "쉬운 말", 약어 금지, `plainKo` "중학생·전문용어 금지" |
| `scripts/workflows/team4-catalyst.js` | 64, 96-97 | 쉬운 말·약어 금지 |
| `scripts/workflows/team5-sector.js` | 78-80 | 약어 금지(WRS 예시) |
| `scripts/workflows/chief-report.js` | 76, 123-133 | 약어 금지, 문단당 숫자 5개 금지, 업계 용어 금지, 비유 1개 이상, 중학생 |
| `scripts/lib/plain-terms.js` | 전체 | TERMS 16-31. `build-chief-report.js:13,66-67`에서 적용(오늘 "맨몸 약어 2곳" 치환) |
| `tests/lib/briefing-plain.test.js` | 2-7, 49-57, 65-72, 107-154 | JARGON 목록, [1]·[5] 단언 |
| `index.html` | 904-906 | Node 산문(mirBrief) 규칙 주석 |
| `RESUME.md` | 91-93, 145 | 설명 |

### 4-2. 교체 방법

- 위 프롬프트 줄을 삭제하고 아래 블록으로 대체. `company`/`easy`/`plainKo` 설명을 "전문 투자자 대상"으로. `plainKo`→`summary`, `easySummary` 삭제, team1 `easy`→`analysis`.
- `plain-terms.js` 삭제, `build-chief-report.js:13,64-67` 삭제.
- `briefing-plain.test.js`: [1]·[5]는 실패하므로 제거. [2](undefined/NaN 유출)·[3](한국어 업종명)·[4](실장/미르 일치)는 유지. JARGON에서 금융 용어 항목을 빼고 **내부 코드 유출**(`evidence_level`, `no_source`, `bounce_trigger|congestion|dryUp`, `RS_\dmo`)만 남긴다.
- chief `marketVerdictKo` 설명을 "시장 국면 판단 + 자금 흐름 + 포지션 지침. 문단 수·숫자 개수 제한 없음"으로.

### 4-3. 교체용 블록 (프롬프트 RULES에 그대로)

```
## 서술 기준 — 독자는 재무·회계 전공의 금융 실무자다
1. 눈높이를 낮추지 마라. 비유·초보자용 요약·"쉽게 말해" 식 풀이는 쓰지 않는다.
2. 표준 용어는 그대로 쓴다: YoY/QoQ, 가이던스, 컨센서스, EPS, FCF, 마진, 백로그, 크랙 스프레드,
   PIPE, 전환사채, 희석, 리레이팅, 밸류에이션, RS 백분위, ADR, 50/200일선 이격 등.
3. 이 시스템 고유 지표(WRS·VOL_X·CLS_POS·BBWTHD·F10d·Congestion)는 처음 한 번만 정의를 붙이고
   이후엔 이름만 쓴다. 예: "VOL_X 3.1(거래대금 20일 평균 대비)" → 이후 "VOL_X 3.1".
4. 원 수치를 생략하지 마라. 제공된 실적표·RS·WRS·이격·거래량 배수·가이던스 수치는 단위와 기간을 붙여
   그대로 인용한다. 문단당 숫자 개수 제한은 없다.
5. 숫자는 quote나 제공 자료에 실제로 있는 것만 쓴다. 근거에 숫자가 없으면 숫자 없이 서술한다.
6. 구조: 결론 → 근거(출처) → 반대 근거/리스크 → 확인할 조건. 각 항목은 필요한 만큼만, 상한은 지킨다.
7. 회사 설명은 사업 모델·매출 구성·핵심 고객·현 국면을 밀도 있게 2~3문장. 교과서식 부연 금지.
8. 판단 근거를 밝히되 매수·매도 권유는 하지 않는다. "무엇이 확인되면 논리가 성립/붕괴하는지"로 끝낸다.
9. 근거가 없으면 "근거 없음"이라고 짧게 쓴다. 분량을 채우기 위한 서술·추측은 금지.
10. 한국어로 쓴다. 영문 고유명사·티커·지표명은 원문 그대로 둔다.
```

### 4-4. 프롬프트의 다른 결함

- `chief:37` "`llmResearched.length`를 그대로 써라" — 모델은 배열 길이를 못 센다. `prepare-chief-args.js`가 `llmResearchedCount`·`llmItemsCount`를 넘겨야 한다(breakoutTotal처럼).
- `chief:77-81,112` todayFocus 3~5 required — 🔴 날에도 강제. 0~5 허용, `action` enum.
- `team4:66` `isHighlight`·`:62` `categoryName`은 Node가 덮어쓰는데 스키마에 남아 있음. 제거.
- `team2:82` `upcomingCatalyst` — "없으면 필드 생략"을 명시.
- 규칙 중복: quote 규칙이 스키마(`team2:61`)와 RULES(93-95)에, 숫자 규칙이 75와 101에 — 한 곳만.

---

## 5. 검증해서 문제없다고 확인한 것

백분위(mid-rank, 결측 null) · WRS 재계산(NaN 제외, 기간별 Count) · ETF 제외 일관성 · ma-guard(거르지 않고 판정불가) · `tracking.evaluate` 3조건 OR·manualHold 우선 · `enforceCap` 보호 순서 · `bars.fetchMany` 예산·백오프·미완성봉 제거 · `regime.crossScan` · Congestion base/breakout/failed/extended 분기 · sector-flow 감쇠 일관·평일 기준 · flow-cross 공집합 유지 · research-rotation 결정적 정렬 · 3팀 breakouts의 `breakoutTotal/Shown` 마커(이 시스템에서 상한 마커가 제대로 된 유일한 곳) · build-chief-report failed/pending 분리 · verify-claims 봇차단 보존·DNS dead 판정 · tryAgent 인덱스 기반 failed · writeAtomic · commitAndPush 무throw.

---

## 6. 실행 순서 제안

| 단계 | 내용 | 효과 |
|---|---|---|
| **1. 숫자 결함** | 1-1 봉 갭 감지·배너 / 1-2 재편입 재검사 + chartCheck·breakouts·live를 최종 status로 필터 / 1-4 돌파봉 거래량 / 1-3 우선주 제외 / 1-6 0종목 throw | 오보 원인 제거. 오늘 chartCheck 4종목 오염, 데드크로스 날짜 변동, NEE-T 재발 방지 |
| **2. 토큰** | 2팀 "TTL 안이면 이월" + 5팀 버킷 + 4팀 ⑥ 재조사 제외 + 프롬프트 상한·숫자 규칙 + 3개 종합 sonnet | 하루 ~300만 → ~150만. 커버리지는 오히려 올라감 |
| **3. 서술 기준** | 4-1 전부 교체, plain-terms 삭제, 테스트 정리, 실장 카운트 필드 | 금융 실무자 눈높이. 테스트 40개 중 [1]·[5] 제거·수정 |
| **4. 원천 데이터** | 3-1 리포트 절 추가(재무제표·증권사 리포트·8-K·4팀 촉매·5팀 근거·자금흐름 표·3팀 추적 표·추정치 열), 3-2 dead LLM 필드 렌더, 실장 인자 확장(`--team1`, count 필드, FINRA 절대값), 대시보드 실장 결과·dataNotice·fetchedAt 표시 | "핵심 원천 데이터 누락 없음" |
| **5. 나머지** | 1-7·1-8·1-9·1-10·1-11, 정리(`_reuse.json` 등), 문서 정정 | 조용한 상한 제거 |

1단계는 반나절, 2·3단계는 각 반나절, 4단계는 하루 남짓으로 본다. 각 단계 뒤 `start breakout` 1회로 회귀 확인.
